# ProLIF Web parity fixes — 2026-09-04

Reference: ProLIF 2.2.1, native RDKit 2025.03.6; Web RDKit 2025.03.4.
Frozen 16-entry local Plinder corpus, unchanged native expected events.

Fixed native bond-insertion neighbor order (including extended-neighbor plane
geometry), named-water exclusion for implicit H bonds, and `includeWater`
behavior. Weaver now supplies residue names to the component API. Exact native
water aliases are preserved, including `H20` (digit zero).

Post-fix report: `web/parity-runs/plinder-20260904-fixed/report.json`.

- Implicit lane: 14,332 successful checks, 576 events in each engine.
- Explicit lane: 18,148 successful checks, 1,758 events in each engine.
- Zero missing/extra events, geometry mismatches, or one-sided errors.
- 592 shared unsupported checks remain: 590 default-radius Fe/Ru checks and
  two implicit-H cases with no heavy-atom neighbor. These are not silently passed.
- The full-coverage gate consequently remains red (14/16 structures fully
  supported); the three frozen defect regressions pass, as do 293 ordinary tests.

This certifies the tested serialized-input rule behavior, not every high-level
Python preparation path. Native pre-serialization versus serialized preparation
still differs (39 rule/pair comparisons: 35 extra events and six geometry
differences, potentially overlapping). Explicit H coordinates are shared, not
generated independently. The RDKit patch versions differ. Rules with no positive
examples are not certified by their zero-event checks.
